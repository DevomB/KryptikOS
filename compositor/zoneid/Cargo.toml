[package]
name = "zoneid"
description = "Kryptik zone visual identity: the perceptual distinctness invariant"
version.workspace = true
edition.workspace = true
license.workspace = true

# No dependencies: the colour maths is published formulae checked against
# reference values in the tests (the ADR-010 argument, as for kryptikd).
[dependencies]

[[bin]]
name = "zoneid"
path = "src/bin/zoneid.rs"
