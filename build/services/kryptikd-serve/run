#!/bin/sh
# The launch daemon: the only path from the desktop session (group kryptik) to
# zone creation, which needs root. Supervised, so a crash restarts it.
exec /usr/bin/kryptikd serve --zones /usr/lib/kryptik/zones --rootfs /var/lib/kryptik/zones
