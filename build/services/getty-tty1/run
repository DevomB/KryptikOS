#!/bin/sh
# Authenticated login on tty1; the desktop session starts from the user's
# profile. Only install media get a passwordless root shell (kryptik-console).
exec /usr/sbin/agetty --noclear tty1 linux
