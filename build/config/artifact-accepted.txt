# Findings of tools/check-artifact-hardening.sh that the image keeps, each with
# its reason: `FINDING PATH # why`, or `RPATH PATH RPATH # why`, since an RPATH
# entry accepts one rpath. PATH and RPATH are globs on the object's path in the
# image and on its rpath. A finding not listed here fails --strict, and so does
# an entry that no longer matches anything.

NO-CET  usr/bin/kryptikd                        # stable rustc marks nothing for CET, nor does the std it links
NO-CET  usr/bin/kryptik-wlproxy                 # the same
NO-CET  usr/lib/libgmp.so.*                     # GMP's hand-written x86-64 assembly carries no CET note

# man-db's programs load its private libraries from /usr/lib/man-db.
RPATH   usr/bin/*                    /usr/lib/man-db     # catman, lexgrog, man, man-recode, mandb, manpath, whatis
RPATH   usr/sbin/accessdb            /usr/lib/man-db     # the same
RPATH   usr/libexec/man-db/*         /usr/lib/man-db     # the same
RPATH   usr/lib/man-db/libmandb-*.so /usr/lib/man-db     # the same, for libman beside it

RPATH   usr/bin/perl*                /usr/lib/perl5/*/CORE   # libperl.so lives in perl's CORE directory (useshrplib)
RPATH   usr/lib/gconv/*.so           $ORIGIN             # glibc's converters load the helper libraries beside them

# The loader's own directory, named again: it adds nothing.
RPATH   usr/lib/libgmpxx.so.*        /usr/lib/../lib     # libtool, linking libgmp from the build
RPATH   usr/lib/libcc1.so.*          /usr/lib/../lib     # libtool, in GCC's build
RPATH   usr/lib/gcc/*/*/plugin/libc[cp]1plugin.so.*  /usr/lib/../lib   # the same
RPATH   usr/lib/libskarnet.so.*      /usr/lib            # skalibs' own link line
RPATH   usr/lib/perl5/*/auto/Time/HiRes/HiRes.so  /usr/lib   # Time::HiRes, linking librt
