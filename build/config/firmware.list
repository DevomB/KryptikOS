# The only firmware shipped under /lib/firmware on the verified root (ADR-012).
# One `find -path` pattern per line, matched in the tree linux-firmware's
# copy-firmware.sh lays out; a selected link brings its target.
# `newest N PATTERN` keeps the N highest trailing -NUMBERs per device, since a
# driver falls back only a few firmware API versions. A pattern that matches
# nothing fails the build.

# Intel Wi-Fi, 7260 to Wi-Fi 7: the newest three API versions, and every PNVM.
newest 3 iwlwifi-*.ucode
iwlwifi-*.pnvm

# Qualcomm Atheros: the laptop parts, by board revision.
ath10k/QCA6174/hw3.0/*
ath10k/QCA9377/hw1.0/*
ath11k/QCA6390/hw2.0/*
ath11k/WCN6855/hw2.0/*
ath12k/WCN7850/hw2.0/*

# MediaTek MT7921 (7961 firmware), MT7922 and MT7925, without their Bluetooth.
mediatek/WIFI_MT7961_patch_mcu_1_2_hdr.bin
mediatek/WIFI_MT7961_patch_mcu_1a_2_hdr.bin
mediatek/WIFI_RAM_CODE_MT7961_1.bin
mediatek/WIFI_RAM_CODE_MT7961_1a.bin
mediatek/WIFI_MT7922_patch_mcu_1_1_hdr.bin
mediatek/WIFI_RAM_CODE_MT7922_1.bin
mediatek/mt7925/WIFI_*

# Realtek: the rtw88 and rtw89 families, and the USB sticks rtl8xxxu drives.
rtw88/*
rtw89/*
rtlwifi/rtl8192cufw*.bin
rtlwifi/rtl8192eu_*.bin
rtlwifi/rtl8723bu_*.bin
rtlwifi/rtl8188eufw.bin

# Broadcom over PCIe (Dell and Apple machines); the SDIO parts are not.
brcm/brcmfmac*-pcie*

# Graphics: all of Intel's and AMD's. Not NVIDIA: nouveau's GSP firmware is tens
# of MB per generation, and the firmware framebuffer still gives a display.
i915/*
xe/*
amdgpu/*
