# Binaries allowed to be setuid or setgid in a Kryptik image; stage 06 takes
# the bit off every other file in its root (tools/audit-setuid.sh --strip).
#   /absolute/path   # justification
# Privilege transitions go through kryptikd, where they are auditable. First
# check whether a file capability or brokered service will do.
/usr/bin/su       # the one way from a login to root: an administrator's, and the test drivers' (vm-drive.py su:)
/usr/bin/passwd   # a user changes their own password; nothing brokers that yet
