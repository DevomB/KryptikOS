# SC2164 ("cd ... || exit"): every script sources build/lib/common.sh, whose
# `set -Eeuo pipefail` and ERR trap already abort on a failed cd. A script
# that does not source it should re-enable the check locally.
disable=SC2164
